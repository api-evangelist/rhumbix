generated: '2026-07-21'
method: derived
source: openapi/rhumbix-public-api-openapi-original.json
format: swagger-responses
note: >-
  Rhumbix Public API responses are plain JSON (application/json); it does not use
  RFC 9457 application/problem+json. Import/export status operations surface an
  ImportStatus/BatchStatus envelope with `status` and `detail` fields. Only the
  status codes below are declared in the Swagger 2.0 spec; the batch-status
  polling operations are where authorization failures surface as 403.
envelope:
  schema: ImportStatus
  fields:
    status: Machine-readable batch/import processing state
    detail: Human-readable description of the status or failure
problems:
- status: 403
  title: Forbidden
  meaning: >-
    The x-api-key is missing, invalid, or not authorized for the requested
    company_key / batch_key.
  remediation: >-
    Verify the x-api-key header and that the key is provisioned for the
    company_key in the request path.
  source_operations:
  - openapi/rhumbix-public-api-openapi-original.json#batchEmployeeImportStatus
  - openapi/rhumbix-public-api-openapi-original.json#batchProjectImportStatus
- status: 202
  title: Accepted (still processing)
  meaning: >-
    The batch export is accepted and still being processed; results are not yet
    ready. Poll the status endpoint until 200.
  remediation: Re-poll the export status endpoint after a short backoff interval.
  source_operations:
  - openapi/rhumbix-public-api-openapi-original.json#batchWorkshiftExportStatus
